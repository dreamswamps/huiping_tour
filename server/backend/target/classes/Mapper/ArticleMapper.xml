<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.Mapper.ArticleMapper">

    <insert id="insert" parameterType="com.example.POJO.Article">
        INSERT INTO article (title, img, description, content, time, is_deleted, publisher_id, publisher_name)
        VALUES (#{title}, #{img}, #{description}, #{content}, #{time}, 0, #{publisher_id}, #{publisher_name})
    </insert>

    <update id="updateById" parameterType="com.example.POJO.Article">
        UPDATE article
        SET title = #{title},
            img = #{img},
            description = #{description},
            content = #{content},
            time = #{time},
            is_deleted = 0
        WHERE id = #{id}
    </update>

    <update id="SoftDeleteByIds">
        UPDATE article
        SET is_deleted = 1
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="UndoByIds">
        UPDATE article SET is_deleted = 0
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <select id="selectById" resultType="com.example.POJO.Article">
        SELECT * FROM article WHERE id = #{id}
    </select>

    <select id="selectAll" resultType="com.example.POJO.Article">
        SELECT * FROM article
        <where>
            is_deleted = 0
            <if test="title != null and title != '' ">
                AND title like concat('%',#{title},'%')
            </if>
        </where>
    </select>


</mapper>
