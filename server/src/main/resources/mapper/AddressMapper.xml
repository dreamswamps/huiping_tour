<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.huiping.server.mapper.AddressMapper">
    <select id="findByUserId" resultType="org.huiping.server.entity.UserAddress">
      SELECT * FROM user_addresses WHERE
        user_id=#{userId} ORDER BY is_default DESC,id DESC
    </select>
    <select id="findById" resultType="org.huiping.server.entity.UserAddress">
      SELECT * FROM user_addresses WHERE
        user_id=#{userId} AND id=#{addressId}
    </select>
    <select id="countByUserId" resultType="int">
      SELECT COUNT(*) FROM user_addresses WHERE user_id=#{userId}
    </select>
    <select id="countDefault" resultType="int">SELECT COUNT(*) FROM user_addresses WHERE user_id=#{userId} AND
        is_default=1
    </select>
    <update id="clearDefault">UPDATE user_addresses SET is_default=0 WHERE user_id=#{userId}</update>
    <update id="setDefault">UPDATE user_addresses SET is_default=1 WHERE id=#{addressId} AND user_id=#{userId}</update>
    <select id="findEarliestExcept" resultType="long">SELECT id FROM user_addresses WHERE user_id=#{userId} AND id !=
        #{excludeId} ORDER BY id ASC LIMIT 1
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">INSERT INTO
        user_addresses(user_id,receiver_name,receiver_phone,province,city,district,detail_address,postal_code,label,is_default)
        VALUES(#{user_id},#{receiver_name},#{receiver_phone},#{province},#{city},#{district},#{detail_address},#{postal_code},#{label},#{is_default})
    </insert>
    <update id="update">UPDATE user_addresses SET
        receiver_name=#{addr.receiver_name},receiver_phone=#{addr.receiver_phone},province=#{addr.province},city=#{addr.city},district=#{addr.district},detail_address=#{addr.detail_address},postal_code=#{addr.postal_code},label=#{addr.label},updated_at=NOW()
        WHERE id=#{addressId} AND user_id=#{userId}
    </update>
    <delete id="delete">DELETE FROM user_addresses WHERE id=#{addressId} AND user_id=#{userId}</delete>
    <update id="promoteFirst">UPDATE user_addresses SET is_default=1 WHERE id=(SELECT id FROM (SELECT id FROM
        user_addresses WHERE user_id=#{userId} ORDER BY id ASC LIMIT 1) x)
    </update>
</mapper>
