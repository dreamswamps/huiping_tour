<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.huiping.server.mapper.AddressMapper">
    <select id="selectByUserId" resultType="org.huiping.server.entity.UserAddress">
        SELECT *
        FROM user_addresses
        WHERE user_id=#{userId}
        ORDER BY is_default DESC,id DESC
    </select>

    <select id="selectByIdAndUserId" resultType="org.huiping.server.entity.UserAddress">
        SELECT *
        FROM user_addresses
        WHERE user_id=#{userId}
        AND id=#{addressId}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*)
        FROM user_addresses
        WHERE user_id=#{userId}
    </select>

    <select id="countDefaultByUserId" resultType="int">
        SELECT COUNT(*)
        FROM user_addresses
        WHERE user_id=#{userId} AND
        is_default=1
    </select>

    <update id="clearDefaultByUserId">
        UPDATE user_addresses
        SET is_default=0
        WHERE user_id=#{userId}
    </update>

    <update id="setDefaultByIdAndUserId">
        UPDATE user_addresses
        SET is_default=1
        WHERE id=#{addressId}
        AND user_id=#{userId}
    </update>

    <select id="findEarliestExcept" resultType="long">
        SELECT id
        FROM user_addresses
        WHERE user_id=#{userId}
        AND id != #{excludeId}
        ORDER BY id ASC LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_addresses(user_id,receiver_name,receiver_phone,province,
        city,district,detail_address,postal_code,label,is_default)
        VALUES(#{userId},#{receiverName},#{receiverPhone},#{province},
        #{city},#{district},#{detailAddress},#{postalCode},#{label},#{isDefault})
    </insert>

    <update id="update">
        UPDATE user_addresses
        SET receiver_name=#{addr.receiverName},receiver_phone=#{addr.receiverPhone},province=#{addr.province},
        city=#{addr.city},district=#{addr.district},detail_address=#{addr.detailAddress},postal_code=#{addr.postalCode},label=#{addr.label},updated_at=NOW()
        WHERE id=#{addressId}
        AND user_id=#{userId}
    </update>

    <delete id="delete">
        DELETE FROM user_addresses
        WHERE id=#{addressId}
        AND user_id=#{userId}
    </delete>

    <update id="promoteFirst">
        UPDATE user_addresses
        SET is_default=1
        WHERE id=(SELECT id FROM
        (SELECT id
        FROM user_addresses
        WHERE user_id=#{userId}
        ORDER BY id ASC LIMIT 1) x)
    </update>
</mapper>
