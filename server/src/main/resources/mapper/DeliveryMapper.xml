<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.huiping.server.mapper.DeliveryMapper">

  <select id="findDeliveryOrder" resultType="map">SELECT id,user_id AS userId,order_no AS orderNo,status,tracking_no
    AS trackingNo,receiver_name,receiver_phone,province,city,district,detail_address FROM orders WHERE id=#{orderId}
  </select>

  <update id="markDelivered">UPDATE orders SET status=2,deliver_time=NOW(),tracking_no=#{trackingNo} WHERE
    id=#{orderId} AND status=1
  </update>

</mapper>
