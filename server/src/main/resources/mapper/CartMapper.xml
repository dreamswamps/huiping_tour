<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.huiping.server.mapper.CartMapper">
  <select id="findByUserId" resultType="org.huiping.server.entity.Cart">
    SELECT * FROM carts WHERE user_id=#{userId} ORDER BY id ASC
  </select>

  <delete id="deleteByUserId">
    DELETE FROM carts WHERE user_id=#{userId}
  </delete>

  <delete id="deleteByProductIds">
    DELETE FROM carts WHERE user_id=#{userId} AND product_id IN
    <foreach collection="productIds" item="pid" open="(" separator="," close=")">
      #{pid}
    </foreach>
  </delete>

  <insert id="insertBatch">
    INSERT INTO carts(user_id, product_id, product_name, product_price, product_thumb, quantity)
    VALUES
    <foreach collection="items" item="item" separator=",">
      (#{userId}, #{item.productId}, #{item.name}, #{item.price}, #{item.thumb}, #{item.quantity})
    </foreach>
  </insert>
</mapper>
